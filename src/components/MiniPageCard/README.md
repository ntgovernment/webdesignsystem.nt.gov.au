# MiniPageCard

Compact card grid aligned to the web-design-system Card component (mini variant).

## Design Tokens

- Icon color uses `--nt-mini-card-icon-color`, which defaults to `--clr-text-emphasis` from the design system.
